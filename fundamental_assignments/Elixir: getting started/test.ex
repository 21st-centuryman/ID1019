
  defmodule Test do
    # Compute the double of a number.
    def double(n) do
      n * 2
    end
    def celsius(n) do
      (n-32)/1.8
    end
    def area(n,m) do
      n * m
    end
    def circle(n) do
      3.141592 * n * n
    end
  end
